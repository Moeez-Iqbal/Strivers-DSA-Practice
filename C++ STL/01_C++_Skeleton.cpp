# include <bits/stdc++.h> // This is provide by C++ so all the libraries stored in this and don't have to write math.h and string.h etc
# include <math.h> // in order to include all libraries of math use math.h
# include <string.h> // in order to include all strings libraries of string  use string .h
using namespace std;

int main () {
    // THis is Skeleton of C++
    return 0;
}